import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Reset your TernHub account password.",
};

export default function ResetPassword({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
