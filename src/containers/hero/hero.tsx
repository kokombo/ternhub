import { HeroTitle, Tags, Search } from "@/components";

const Hero = () => {
  return (
    <section className="hero">
      <HeroTitle />

      <Tags />

      <Search />
    </section>
  );
};

export default Hero;
